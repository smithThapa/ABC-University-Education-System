extends ../components/_templateBase

block append head
    link(rel="stylesheet" type="text/css" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.1.3/css/bootstrap.css")
    link(rel="stylesheet" type="text/css" href="https://cdn.datatables.net/1.10.19/css/dataTables.bootstrap4.min.css")

block append scripts
    script(src='https://cdn.datatables.net/1.10.19/js/jquery.dataTables.min.js')
    script(src='https://cdn.datatables.net/1.10.19/js/dataTables.bootstrap4.min.js')
    script(src='/js/databaseDesignJQuerry.js')

block content
    +breadcrumbDisplay([['Home','/home'],['News']])

    h2 News

    -if(news.length<1)
        h4.font-weight-bold.text-dark.mt-0.ml-4 No News in ABC University.
    -else
        table.table#dataTableNews.table.table-borderless.table-sm
            thead
                tr
                    th.text-right.px-5 Sort By Date
            tbody
                each newElement in news
                    tr
                        td
                            p.d-none= newElement.createdAt
                            //- -const creationDate = forum.createdAt.format();
                            -const creationDate = moment(newElement.createdAt).format('dddd DD MMM YYYY HH:mm');

                            +cardDisplayItems(newElement.type, `${newElement.title}`, `${newElement.user.firstName} ${newElement.user.lastName}`, creationDate, `/news/${newElement.slug}`, 'New')